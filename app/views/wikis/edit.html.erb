<h1>Edit wiki</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines</p>
    <ul>
      <li>Be as descritpive as possible</li>
      <li>Make sure to format the information input</li>
      <li>Markdown language is supported</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @wikis do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 80, class: 'form-control', placeholder: "Enter wiki body" %>
      </div>
      <div class="form-group">
        <%= f.submit "save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
